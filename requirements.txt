# Verified working set (2026-06-29). ADK pulls a2a-sdk + mcp via the [a2a] extra.
google-adk[a2a]==2.3.0
mcp==1.28.1
a2a-sdk==0.3.26
uvicorn>=0.30
httpx>=0.27
python-dotenv>=1.0
google-cloud-secret-manager>=2.20   # write rotated refresh token back (scale-to-zero)
# Optional: use Claude instead of Gemini (MODEL=anthropic/claude-sonnet-4-6)
litellm>=1.40
